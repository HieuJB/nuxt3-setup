import matchs, { type RepoMatch } from '@/api/match'

/*
- example
- todo
*/

export interface RepositoryProps {
  matchs: RepoMatch
}

const apis: RepositoryProps = {
  matchs
}

export default apis
